# Init Command

Pre-load templates, CSS, and course configs into session cache. No arguments needed.

## Steps

1. Run: `node scripts/session-init.js`
2. Display the output confirming what was cached.
